version: '3.8'

services:
  cryptconnect:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: cryptconnect
    restart: unless-stopped
    ports:
      - "8080:80"
      - "8443:443"
    volumes:
      - ./certs:/etc/nginx/certs:ro
