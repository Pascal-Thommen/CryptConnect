services:
  cryptconnect:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: cryptconnect
    ports:
      - "8080:80"
      - "8443:443"
    restart: unless-stopped
    networks:
      - web

networks:
  web:
    external: true
